<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>DOCUMENT</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="forms p-10 text-white tracking-wide">
            <form action="/create" method="post">
                <input class="block w-full rounded-lg outline-none px-5 py-2 bg-zinc-800" type="text" placeholder="Title goes here" name="title">
                <textarea class="block w-full rounded-lg outline-none mt-2 px-5 py-2 resize-none bg-zinc-800" placeholder="Write your task details" name="details"></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-lg" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 flex fle<x-wrap gap-4">
            <% if(files.length > 0){ %>
                <% files.forEach(function(val){ %>
                    <div class ="w-72 p-3 bg-zinc-800 rounded-lg ">
                        <h3 class="text-white text-3xl"><%= val %></h3>
                        <a class="text-blue-500 inline-block mt-3" href="">Read more</a>
                    </div>
                <% })%>
            <% }else{ %>
                <h3 class="text-zinc-600 font-semibold">NO TASK YET</h3>
            <% } %>
        </div>
    </div>
<script src="/javascripts/script.js"></script>
</body>
</html>