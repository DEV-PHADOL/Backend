<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>  
    <div class="flex justify-center justify-between w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-3/4">
            <h3 class="text-3xl mb-5">Hello, <span class="capitalize"><%= user.name%></span></h3>
            <h5 class="mb-5">you can create a new post</h5>
            <form method="post" action="/post">
                <textarea name="content" class="block p-3 w-1/2 resize-none outline-none bg-transparent border-2 border-zinc-800 rounded-lg" placeholder="What's on your mind ? "></textarea>
                <input class="block px-3 py-2 bg-blue-500 rounded-lg mt-3" type="submit" value="Create Post">
            </form>
            <div class="posts mt-12">
                <h3 class="text-zinc-400 ">Your Posts.</h3>
                <div class="post">
                    <% user.posts.reverse().forEach(function(post){ %>
                        <div class="post w-1/2 p-3 rounded-md border-2 border-zinc-800 mt-5">
                            <h4 class="text-blue-500 mb-2">@<%= user.username%></h4>
                            <p><%= post.content %></p>
                            <div class="flex mt-5 gap-4">
                                <a class="text-blue-500 " href="">Like</a>
                                <a class="text-zinc-500" href="">Edit</a>
                            </div>
                        </div>                        
                    <% })%>
                </div>
            </div>
        </div>
        <div>
            <a class="bg-red-500 rounded-md px-3 py-2" href="/logout">Logout</a>
        </div>
    </div>
</body>
</html>